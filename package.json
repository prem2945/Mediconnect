{
  "name": "mediconnect-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run build",
    "build": "npm --prefix MediConnect/client install && npm --prefix MediConnect/client run build && npm --prefix MediConnect/server install",
    "start": "npm --prefix MediConnect/server start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
