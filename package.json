{
  "name": "mediconnect-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd MediConnect/server && npm install",
    "start": "cd MediConnect/server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
